#!/bin/sh

ROOT_PATH=$(dirname "$(cd "$(dirname "$0")"; pwd)")

echo Notihub root path: ${ROOT_PATH}

cd ${ROOT_PATH}/api
git pull
cd ${ROOT_PATH}/push
git pull
cd ${ROOT_PATH}/web
git pull
cd ${ROOT_PATH}
git pull
